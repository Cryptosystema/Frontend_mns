# 🚀 Автоматические коммиты - Инструкция

## Проблема
VSCode в dev container часто теряет доступ к терминалу (ENOPRO ошибка), из-за чего git команды не работают.

## Решение

Созданы **4 способа** быстрых коммитов:

---

### 1️⃣ Bash скрипт `./autocommit` (РЕКОМЕНДУЕТСЯ)

**Самый надежный способ!** Работает даже при падении терминала.

```bash
# Сделать скрипт исполняемым (один раз):
chmod +x autocommit

# Использование:
./autocommit "feat: add new feature"        # С твоим сообщением
./autocommit --auto                         # Автосообщение с timestamp
```

---

### 2️⃣ Git aliases (после setup)

```bash
# Настройка (один раз):
chmod +x .git-auto-setup.sh
./.git-auto-setup.sh

# Использование:
git qc "твоё сообщение"     # quick commit + push
git ac                      # auto commit с timestamp
git ca                      # commit с редактором + push
```

---

### 3️⃣ VSCode Tasks (Ctrl+Shift+B)

1. Нажми **Ctrl+Shift+B** (или Cmd+Shift+B на Mac)
2. Выбери **"Quick Commit & Push"**
3. Введи сообщение коммита в редакторе
4. Сохрани и закрой - автоматически запушится!

Или выбери **"Auto Commit"** для коммита без редактора.

---

### 4️⃣ Простой скрипт `./quick-commit.sh`

```bash
chmod +x quick-commit.sh
./quick-commit.sh "commit message"
```

---

## VSCode настройки

Обновлены настройки в [.vscode/settings.json](.vscode/settings.json):
- ✅ `git.postCommitCommand: "push"` - автопуш после коммита
- ✅ `git.enableSmartCommit: true` - умные коммиты
- ✅ `git.confirmSync: false` - без подтверждений sync

---

## Что делать если терминал упал?

```bash
# В НОВОМ терминале:
cd /workspaces/mns-terminal
./autocommit --auto
```

Скрипт `autocommit` создан специально для работы в нестабильном окружении!

---

## Быстрая шпаргалка

```bash
# Лучший вариант:
./autocommit "feat: my changes"

# Если терминал работает:
git qc "my changes"

# VSCode GUI:
Ctrl+Shift+B → "Quick Commit & Push"
```

---

**Файлы:**
- `autocommit` - Основной надежный скрипт ⭐
- `quick-commit.sh` - Простой вариант
- `.git-auto-setup.sh` - Настройка git aliases
- `.vscode/tasks.json` - VSCode tasks
- `.vscode/settings.json` - Настройки git для VSCode

---

💡 **Совет:** Добавь в `.bashrc`:
```bash
alias commit='./autocommit'
```

Тогда будет просто: `commit "my message"`
