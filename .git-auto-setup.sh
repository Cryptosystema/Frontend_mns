#!/bin/bash
# Автоматическая настройка git для быстрых коммитов

echo "🔧 Настраиваю git aliases..."

# Алиас для быстрого коммита и пуша
git config --global alias.qc '!f() { git add -A && git commit -m "$1" && git push; }; f'

# Алиас для автокоммита без сообщения (генерирует на основе изменений)
git config --global alias.ac '!git add -A && git commit -m "chore: auto commit $(date +%Y-%m-%d\ %H:%M)" && git push'

# Алиас для коммита всех изменений с интерактивным редактором
git config --global alias.ca '!git add -A && git commit && git push'

echo "✅ Готово! Теперь используй:"
echo ""
echo "  git qc \"сообщение\"  - Быстрый коммит с твоим сообщением"
echo "  git ac               - Автоматический коммит с timestamp"
echo "  git ca               - Коммит с редактором + push"
echo ""
